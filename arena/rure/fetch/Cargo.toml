[package]
name = "casei-rure-fetch"
version = "0.0.0"
edition = "2021"
publish = false

# Direct exact requirements make the checked lockfile also fetch the source
# trees that the audited rure build copies and patches below.
[dependencies]
rure = "=0.2.5"
regex = "=1.13.1"
regex-automata = "=0.4.18"
aho-corasick = "=1.1.5"
memchr = "=2.8.3"
libc = "=0.2.189"
